# The domains list lives in Networks-for-Humanity/fabric-onboarding, not here.
# fabric-website is public, so a copy committed here would be public too.
fabric.domains.txt

# Never commit the data-repo token to a public repository.
.env
.env*.local
.vercel
